name: Create HTML Artifact and Comment on PR

on:
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  build-and-comment:
    runs-on: self-hosted

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Generate HTML file
      run: |
        echo "<html><body><h1>Hello, World!</h1></body></html>" > report.html

    - name: Upload HTML artifact
      uses: actions/upload-artifact@v3
      with:
        name: report
        path: report.html

    - name: Get Artifact URL
      id: artifact_url
      run: |
        ARTIFACT_URL=$(curl -s https://api.github.com/repos/${{ github.repository }}/actions/artifacts | jq -r '.artifacts[] | select(.name == "report") | .archive_download_url')
        echo "::set-output name=url::$ARTIFACT_URL"

    - name: Post comment with artifact URL
      uses: actions/github-script@v6
      with:
        script: |
          const artifactUrl = '${{ steps.artifact_url.outputs.url }}';
          github.issues.createComment({
            owner: context.repo.owner,
            repo: context.repo.repo,
            issue_number: context.issue.number,
            body: `The HTML report is available [here](${artifactUrl}).`
          })
